# Product Requirements Document

## 1. 문서 상태

- 제품명: **한글변환**
- 저장소/실행 파일 가칭: `gksrmfqusghks` / `HanKey`
- 상태: Active — production v1 implementation contract
- 작성일: 2026-08-26
- 우선 플랫폼: macOS 14 Sonoma 이상, Apple Silicon 및 Intel
- 입력 소스 범위: macOS ABC ↔ 2-Set Korean

제품명과 최소 지원 버전은 배포 전 변경할 수 있지만, 로컬 전용·정밀도 우선·보안 필드 무관찰 원칙은 변경 불가 요구사항입니다.

## 2. 문제

한국어와 영어를 번갈아 쓰는 사용자는 현재 입력 소스를 확인하지 못한 채 물리 키 위치가 맞는 다른 언어를 입력합니다. 오류를 발견한 뒤에는 단어 삭제, 한/영 전환, 재입력을 반복해야 합니다. 기존 자동 교정은 일반 오타에는 대응하지만 입력 레이아웃 착오는 별도 문제이며, 전역 키 입력을 다루는 도구는 개인정보 보호에 대한 신뢰 부담이 큽니다.

## 3. 목표

1. 잘못된 입력 소스로 입력한 단어를 사용 흐름을 끊지 않고 복구합니다.
2. 교정 후 시스템 입력 소스를 의도한 언어로 전환해 같은 실수를 연속해서 만들지 않게 합니다.
3. 자동 교정이 위험한 문맥에서는 아무것도 하지 않습니다.
4. 모든 판단을 기기에서 수행하고 원문 키 입력을 저장하지 않습니다.
5. 권한, 호환성, 실패 상태를 숨기지 않는 프로덕션 수준의 macOS 경험을 제공합니다.

## 4. 비목표

- 일반 맞춤법·문법 교정
- 번역, 문장 재작성 또는 클라우드 AI
- 모바일·Windows·Linux 지원
- 두벌식 이외의 한글 자판 또는 ABC 이외의 영문 배열을 v1에서 자동 지원
- 비밀번호, 보안 입력, 원격 데스크톱 세션 내부 텍스트 교정
- 모든 앱과 커스텀 렌더링 편집기의 완전한 호환성 주장

## 5. 주요 사용자

### 한국어 중심 이중언어 사용자

- 메신저·문서·메일에서 한국어와 영어를 자주 전환합니다.
- 생각의 흐름을 유지하면서 한/영 전환 실수를 복구하고 싶습니다.

### 개발자·지식 근로자

- 자연어와 코드·경로·식별자를 같은 시간대에 입력합니다.
- 터미널에서는 명시적으로 켠 전용 안전 모드로 자연어 입력 실수를 복구하고, 명령·경로·옵션은 자동 변형하지 않아야 합니다.

### 개인정보 보호에 민감한 사용자

- 키보드 권한을 요구하는 앱을 기본적으로 신뢰하지 않습니다.
- 네트워크 미사용, 짧은 메모리 버퍼, 확인 가능한 저장 데이터가 필요합니다.

## 6. 핵심 사용자 흐름

### 6.1 첫 실행

1. 앱이 무엇을 관찰하고 무엇을 저장하지 않는지 먼저 설명합니다.
2. 샘플 입력 필드에서 기능을 권한 없이 체험합니다.
3. 입력 모니터링과 손쉬운 사용 권한을 각각 필요한 시점에 요청합니다.
4. 권한 상태를 실시간으로 확인하고 누락된 권한의 시스템 설정 위치로 안내합니다.
5. 자동 교정은 사용자가 명시적으로 켠 뒤 시작합니다.

### 6.2 고신뢰 자동 교정

1. 사용자가 `gksrmffh`를 입력합니다.
2. Space, Enter 또는 문장부호로 단어를 종료합니다.
3. 앱은 원문과 `한글로` 후보를 로컬에서 비교합니다.
4. 고신뢰이며 안전 필터를 통과하면 단어를 `한글로`로 교체합니다.
5. 구분 문자를 보존하고 입력 소스를 2-Set Korean으로 전환합니다.
6. 짧은 비침해 알림으로 교정 사실과 되돌리기 방법을 알립니다.

### 6.3 수동 교정

1. 자동 판정이 보류됐거나 자동 교정이 꺼져 있습니다.
2. 사용자가 설정한 단축키를 누릅니다.
3. 마지막 단어 또는 현재 선택 영역을 반대 레이아웃으로 변환합니다.
4. 다시 누르면 원상 복구합니다.

### 6.4 잘못된 교정 되돌리기

1. 사용자가 전용 Undo 단축키 또는 메뉴 막대 명령을 실행합니다.
2. 텍스트와 입력 소스를 가능한 범위에서 이전 상태로 복구합니다.
3. 해당 원문/후보 쌍을 로컬 `Never convert` 학습 데이터에 제안합니다.

### 6.5 교정 결과를 지우고 다시 입력

1. 사용자가 자동 교정된 결과 전체를 같은 필드에서 Backspace로 삭제합니다.
2. 사용자가 같은 물리 키 시퀀스를 다시 입력합니다.
3. 앱은 이번 입력을 보존하고 해당 원문/후보 쌍을 로컬 `Never convert` 목록에 저장합니다.
4. 단순 연속 입력, 부분 삭제, 다른 입력이나 포커스 이동은 학습으로 간주하지 않습니다.

## 7. 기능 요구사항

### FR-01 권한과 상태

- 입력 모니터링, 손쉬운 사용, 로그인 시 실행 상태를 독립적으로 표시합니다.
- 권한 철회, 앱 업데이트로 인한 서명 변경, Secure Keyboard Entry 활성화를 감지합니다.
- 필요한 권한이 없으면 자동 교정은 중단되며 수동 복사 fallback을 임의로 실행하지 않습니다.

### FR-02 입력 관찰과 단어 버퍼

- 물리 키코드, 수정 키, 현재 입력 소스, 앱 전환과 포커스 변화를 처리합니다.
- 버퍼에는 현재 단어에 필요한 최소 상태만 유지합니다.
- 앱 전환, 포커스 이동, 마우스 클릭, 탐색 키, 명령 단축키, 유휴 제한, 최대 길이 초과 시 버퍼를 폐기합니다.
- 합성한 이벤트는 sentinel로 식별해 재관찰 루프를 방지합니다.

### FR-03 결정적 한↔영 변환

- ABC 키 위치에서 두벌식 자모를 만들고 표준 현대 한글 음절로 조합합니다.
- 완성형 한글과 호환 자모를 두벌식 물리 키 위치의 ASCII로 역변환합니다.
- Shift 쌍자음·대문자, 겹모음, 겹받침, 받침 이동, 독립 자모를 테스트합니다.
- 변환은 가역 가능한 입력 범위를 명시하고 불가능한 문자는 보존 또는 보류합니다.

### FR-04 탐지와 신뢰도

- 원문과 변환 후보를 사전, 문자 n-gram, 한글 조합 품질, 문맥 언어, 현재 입력 소스, 앱 선호도, 사용자 예외로 비교합니다.
- 자동 교정은 후보가 원문보다 설정된 안전 마진 이상 우수할 때만 실행합니다.
- 자동 판정 임계값은 수동 판정보다 보수적이어야 합니다.
- 사용자 텍스트나 전체 문장을 학습 데이터로 영구 저장하지 않습니다.

### FR-05 강제 안전 필터

- `AXSecureTextField`, Secure Keyboard Entry, 잠금 화면에서는 관찰·학습·교정을 중단합니다.
- URL, 이메일, IP, MAC 주소, UUID, 해시, 경로, 숫자 중심, 혼합 구두점, camelCase, snake_case, ALL_CAPS, 고엔트로피 토큰은 자동 교정하지 않습니다.
- 터미널은 기본 꺼짐인 전용 모드에서 Space로 끝난 고신뢰 자연어 토큰만 지원합니다.
- IDE, 비밀번호 관리자, 원격 데스크톱 앱은 기본 자동 교정 제외 목록에 포함합니다.
- 사용자는 앱, 단어, 입력 필드별 동작을 추가로 제한할 수 있습니다.

### FR-06 텍스트 교체와 입력 소스

- v1 자동 교정은 기본적으로 단어 경계에서 실행합니다.
- 현재 편집기가 노출한 선택 범위를 사용해 원래 단어만 교체하고 서식·주변 텍스트를 보존합니다.
- 클립보드를 기본 경로로 사용하지 않습니다.
- 텍스트 교체 성공 이후에만 목표 입력 소스를 선택합니다.
- 다음 실제 키가 입력 소스 변경보다 먼저 도착하면 짧게 직렬화하고 손실 없이 재전달합니다.

### FR-07 수동 변환과 Undo

- 마지막 단어 변환, 선택 영역 변환, 마지막 교정 되돌리기를 각각 설정 가능한 단축키로 제공합니다.
- 단축키 충돌을 감지하고 기본값을 강제하지 않습니다.
- 전역 앱 Undo 스택을 신뢰하지 않고 앱 자체의 한 단계 교정 기록을 메모리에 유지합니다.

### FR-08 설정과 학습

- 자동 교정, 터미널 전용 모드, 알림, 효과음, 로그인 실행을 독립 토글로 제공합니다.
- `Never convert`, `Always convert`, 앱 제외 목록을 조회·편집·초기화할 수 있습니다.
- 앱 제외는 번들 ID 입력이 아니라 로컬 설치 앱의 이름·아이콘 검색 선택기로 제공합니다.
- `Never convert`는 별도 설정 탭에서 직접 추가·조회·삭제할 수 있어야 합니다.
- 저장되는 데이터의 위치와 내용을 앱 안에서 확인할 수 있습니다.
- 원문 문장이나 키 입력 기록을 내보내는 기능은 제공하지 않습니다.

### FR-09 진단과 호환성

- 메뉴 막대 상태는 정상, 일시중지, 권한 필요, Secure Input, 오류를 구분합니다.
- 콘텐츠를 포함하지 않는 로컬 진단 보고서를 생성할 수 있습니다.
- 지원하지 않는 편집기에서는 무음 실패 대신 비침해 상태를 표시합니다.

## 8. 비기능 요구사항

### 개인정보와 보안

- 런타임 네트워크 요청 0건.
- 현재 토큰 원문은 메모리 밖으로 기록하지 않습니다.
- 보안 입력 상태에서 버퍼 길이는 항상 0이어야 합니다.
- 디버그 로그에도 문자, 키코드 연속열, 선택 텍스트를 포함하지 않습니다.

### 성능

- 이벤트 tap 콜백은 변환·사전 조회·Accessibility IPC를 직접 수행하지 않습니다.
- 단어 경계 판단부터 교정 개시까지 로컬 성능 테스트 p95 50ms 미만을 목표로 합니다.
- 메뉴 막대 유휴 CPU는 측정 가능한 지속 폴링 없이 event-driven으로 유지합니다.

### 품질

- 적대적 코퍼스에서 자동 교정 precision 99.9% 이상을 출시 목표로 하며 recall은 이보다 후순위입니다.
- 자동 교정 오탐 1건은 미탐 다수보다 높은 심각도로 처리합니다.
- 핵심 변환과 판단 엔진은 플랫폼 UI 없이 결정적 단위 테스트가 가능해야 합니다.

### 접근성

- 설정과 온보딩은 키보드만으로 완료할 수 있어야 합니다.
- VoiceOver 레이블, 논리적 포커스 순서, 충분한 대비, Reduce Motion을 지원합니다.

## 9. 출시 승인 기준

- [ ] `gksrmffh → 한글로`, `ㅛㅐㅜㄴ댜 → yonsei` 양방향 E2E 통과
- [ ] URL·이메일·UUID·해시·코드 식별자 적대 테스트에서 자동 교정 0건
- [ ] 보안 필드 및 Secure Keyboard Entry에서 관찰·저장·교정 0건
- [ ] TextEdit, Notes, Mail, Safari/Chrome 일반 입력, Slack 등 대표 앱 호환성 기록
- [ ] Terminal/cmux 전용 모드의 Space 경계 성공 및 Enter·경로·옵션 실패 폐쇄 확인
- [ ] 주요 IDE, 비밀번호 관리 앱 기본 제외 확인
- [ ] 권한 허용·거부·철회·업데이트 상태 UI 완성
- [ ] 전용 Undo와 예외 학습 완성
- [ ] Swift 단위·통합 테스트, 정적 분석, Release 빌드 통과
- [ ] 네트워크 API 금지 검사와 런타임 네트워크 관찰 통과
- [ ] Apple Silicon 및 Intel Release artifact 생성
- [ ] Developer ID 서명·공증은 자격 증명이 제공된 경우 통과; 아니면 재현 가능한 릴리스 스크립트까지 완료
- [ ] README, 개인정보 설명, 호환성표, 릴리스 노트 완성

## 10. 성공 측정

제품은 텔레메트리를 사용하지 않습니다. 품질 평가는 저장소의 버전된 테스트 코퍼스, 로컬 성능 벤치마크, 수동 호환성 매트릭스, 사용자가 명시적으로 제공한 콘텐츠 제거 진단 보고서로 수행합니다.

## 11. 명시적 가정과 열린 결정

- v1 사용자 표시 이름은 ‘한글변환’으로 가정합니다.
- macOS 14+를 기본으로 하되 API·배포 비용이 작으면 macOS 13으로 낮춥니다.
- 첫 배포는 Mac App Store가 아닌 Developer ID 서명·공증 DMG를 우선합니다.
- 영어 사전과 한국어 빈도 데이터의 라이선스 검토가 끝나기 전에는 저장소에 대형 언어 자산을 추가하지 않습니다.
- 제품 자동 교정은 opt-in으로 시작하며 충분한 QA 이후에도 이 기본값을 유지합니다.
